FROM python:3.12-slim

WORKDIR /app

# Copy your Python code into the container.
COPY vm.py ./

# (Optional) Copy a requirements.txt if you add any dependencies.
# COPY requirements.txt ./
# RUN pip install --no-cache-dir -r requirements.txt

CMD ["python", "vm.py"]
